``` mermaid
sequenceDiagram
    participant browser
    participant server

    Note over browser: User types a note in the text field
    Note over browser: User clicks the "Save" button

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note
    activate server
    Note right of server: Server processes the new note
    server-->>browser: 201 Created (or the new note data)
    deactivate server

    Note right of browser: The browser updates the UI to display the new note
```
